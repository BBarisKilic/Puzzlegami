import 'package:flutter/material.dart';

class PuzzleSmallView extends StatelessWidget {
  const PuzzleSmallView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.red,
      ),
    );
  }
}
